---
title: avatar_harness.tools
---

Typed, phase-gated tools the model acts through (§10).

## Functions
### `default_registry() -> ToolRegistry`

A registry with the tier-0 read tools registered (Phase 1).
